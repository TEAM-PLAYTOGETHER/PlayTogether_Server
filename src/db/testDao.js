const getUserNameById = async () => {
  /*
    const sql = 'select * from user where id = ?';
    const name = sql.executeQuery();
    return name;
    */
  return '김플투';
};

module.exports = {
  getUserNameById,
};
